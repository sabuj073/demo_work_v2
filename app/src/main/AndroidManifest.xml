<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="payment.production.payment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Payment">
        <activity android:name=".UserPaymentConfirmActivity"></activity>
        <activity android:name=".EditMyselfActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".EditClientActivity" />
        <activity android:name=".ClientListActivity" />
        <activity android:name=".AdminMessagesActivity" />
        <activity android:name=".AdminReportActivity" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".CreateClientActivity" />
        <activity android:name=".Admin_Interface_Activity" />
        <activity android:name=".UserPaymentActivity" />
        <activity android:name=".UserActivity" />
        <activity
            android:name=".Splash"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
    </application>

</manifest>